# TEMPLATE — NÃO COMMITAR COM VALORES REAIS
# Aplicar na VPS:
#   kubectl create secret generic auth-demo-secrets -n demos \
#     --from-literal=POSTGRES_USER=auth_demo \
#     --from-literal=POSTGRES_PASSWORD=<senha-forte> \
#     --from-literal=JWT_PRIVATE_KEY="$(cat /path/to/private.pem)" \
#     --from-literal=JWT_PUBLIC_KEY="$(cat /path/to/public.pem)"

apiVersion: v1
kind: Secret
metadata:
  name: auth-demo-secrets
  namespace: demos
type: Opaque
stringData:
  POSTGRES_USER: "SUBSTITUIR"
  POSTGRES_PASSWORD: "SUBSTITUIR"
  JWT_PRIVATE_KEY: "SUBSTITUIR_COM_PEM_PRIVADO"
  JWT_PUBLIC_KEY: "SUBSTITUIR_COM_PEM_PUBLICO"
